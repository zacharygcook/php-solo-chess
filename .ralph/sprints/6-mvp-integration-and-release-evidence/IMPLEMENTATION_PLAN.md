# Implementation plan

1. Audit every required acceptance criterion against executable evidence and close only concrete gaps.
2. Add a deterministic end-to-end untimed account/save/history/replay/PGN journey.
3. Add a deterministic timed refresh/increment/timeout/termination journey.
4. Cover responsive/accessibility/browser interaction and failure-recovery paths in the canonical
   clean-clone check without adding hosted infrastructure.
5. Reconcile architecture/API/runbooks/release/scorecard documentation, package a local release
   candidate, and prove the final canonical gate from a clean state.

Use the relevant repository skill for any domain rule changes. Do not lower quality, coverage,
security, dependency, or complexity gates to manufacture completion.
