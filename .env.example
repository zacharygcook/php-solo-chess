# PHP Solo Chess currently requires no environment variables or secrets for default local use.
# Optional: point one process at a non-default local SQLite file.
SOLO_CHESS_DATABASE_PATH=backend/storage/solo-chess.sqlite
